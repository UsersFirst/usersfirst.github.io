{
  "name": "usersfirst-site",
  "version": "1.0.0",
  "description": "Users First website + blog/newsletter build pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-blog.mjs",
    "build:drafts": "DRAFTS=1 node scripts/build-blog.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
